{
  "name": "phpcs",
  "description": "PHP CodeSniffer for Visual Studio Code",
  "version": "1.1.0",
  "author": "Ioannis Kappas",
  "publisher": "johnrdorazio",
  "contributors": [
    {
      "email": "sheva@shevaua.info",
      "name": "Igor Sheviakov"
    },
    {
      "email": "priest@johnromanodorazio.com",
      "name": "John R. D'Orazio"
    }
  ],
  "license": "MIT",
  "galleryBanner": {
    "color": "#6082bb",
    "theme": "dark"
  },
  "icon": "phpcs_icon.png",
  "homepage": "https://github.com/JohnRDOrazio/vscode-phpcs/blob/master/README.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/JohnRDOrazio/vscode-phpcs.git"
  },
  "bugs": {
    "url": "https://github.com/JohnRDOrazio/vscode-phpcs/issues"
  },
  "sponsor": {
    "url": "https://github.com/sponsors/JohnRDOrazio"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "engines": {
    "node": ">=20",
    "vscode": "^1.106.0"
  },
  "activationEvents": [
    "onLanguage:php"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "PHP CodeSniffer configuration",
      "properties": {
        "phpcs.enable": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Control whether phpcs is enabled for PHP files or not."
        },
        "phpcs.executablePath": {
          "scope": "resource",
          "type": "string",
          "default": null,
          "description": "Optional. The path to the phpcs executable."
        },
        "phpcs.composerJsonPath": {
          "scope": "resource",
          "type": "string",
          "default": "composer.json",
          "description": "The path to composer.json"
        },
        "phpcs.standard": {
          "scope": "resource",
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Optional. The name or path of the coding standard to use. Defaults to the one set in phpcs global config."
        },
        "phpcs.autoConfigSearch": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Automatically search for any `phpcs.xml`, `phpcs.xml.dist`, `phpcs.ruleset.xml` or `ruleset.xml` file to use as configuration. Overrides custom standards defined above."
        },
        "phpcs.ignorePatterns": {
          "scope": "resource",
          "type": "array",
          "items": {
            "type": "string",
            "title": "A glob pattern to skip files and folders that match."
          },
          "default": [],
          "description": "An array of glob patterns to skip files and folders that match when linting your documents."
        },
        "phpcs.ignoreSource": {
          "scope": "resource",
          "type": "array",
          "items": {
            "type": "string",
            "title": "Source of the file"
          },
          "default": [],
          "description": "An array of ignored sources. It could be file, gitfs or another file system of any extension"
        },
        "phpcs.errorSeverity": {
          "scope": "resource",
          "type": "number",
          "default": 5,
          "minimum": 0,
          "description": "The minimum severity an error must have to be displayed."
        },
        "phpcs.warningSeverity": {
          "scope": "resource",
          "type": "number",
          "default": 5,
          "minimum": 0,
          "description": "The minimum severity a warning must have to be displayed."
        },
        "phpcs.showWarnings": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Control whether warnings are displayed."
        },
        "phpcs.showSources": {
          "scope": "resource",
          "type": "boolean",
          "default": false,
          "description": "Show sniff source codes in diagnostic messages."
        },
        "phpcs.trace.server": {
          "scope": "window",
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Traces the communication between VSCode and the language server."
        },
        "phpcs.lintOnType": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Lint document when typing"
        },
        "phpcs.lintOnOpen": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Lint document when open it"
        },
        "phpcs.lintOnSave": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Lint document when save it"
        },
        "phpcs.queueBuffer": {
          "scope": "resource",
          "type": "integer",
          "default": 10,
          "description": "Size of queued documents"
        },
        "phpcs.lintOnlyOpened": {
          "scope": "resource",
          "type": "boolean",
          "default": true,
          "description": "Lint only opened files"
        }
      }
    }
  },
  "scripts": {
    "build": "vsce package",
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "test": "mocha -r ts-node/register --ui tdd --timeout 10000 test/**/*.test.ts",
    "clean": "rimraf out"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.0",
    "@vscode/test-electron": "^2.4.1"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
